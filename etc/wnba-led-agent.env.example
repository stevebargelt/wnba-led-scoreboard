# Example /etc/wnba-led-agent.env
# Copy this file to /etc/wnba-led-agent.env on the Pi and edit values.

# Supabase project settings (Dashboard → Settings → API)
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_ANON_KEY=ey...
SUPABASE_REALTIME_URL=wss://YOUR-PROJECT.supabase.co/realtime/v1/websocket

# Device identity
# DEVICE_ID must be the UUID from your Supabase table public.devices.id
# Find it in Dashboard → Database → Table Editor → public.devices → copy the id of your row
DEVICE_ID=00000000-0000-0000-0000-000000000000

# Optional: device-scoped JWT that includes a device_id claim
# DEVICE_TOKEN=ey...

# Local paths/services on the Pi
CONFIG_PATH=/home/pi/wnba-led-scoreboard/config/favorites.json
SCOREBOARD_SERVICE=wnba-led.service
